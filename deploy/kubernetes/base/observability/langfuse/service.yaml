apiVersion: v1
kind: Service
metadata:
  name: langfuse
  namespace: demo
  labels:
    app: langfuse
    component: observability
spec:
  type: ClusterIP
  selector:
    app: langfuse
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
